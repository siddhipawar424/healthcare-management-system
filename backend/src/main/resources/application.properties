# Auto-import local credentials (gitignored - never committed)
spring.config.import=optional:classpath:application-local.properties

# Defaults (overridden by application-local.properties locally)
spring.datasource.url=jdbc:mysql://localhost:3306/healthcare
spring.datasource.username=root
spring.datasource.password=your_password_here

spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

server.port=8080

# JWT Configuration
jwt.secret=your_jwt_secret_key_min_32_chars_here
jwt.expiration=86400000